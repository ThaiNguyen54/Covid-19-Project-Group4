@model IEnumerable<Covid_19_WebApp.Models.Entry_Detail>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EntryID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EntryID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VaccineID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VaccineID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VaccineID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
